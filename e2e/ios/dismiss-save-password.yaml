appId: ${APP_ID}
---
# Conditional iOS system-prompt dismissal. Invoked through runFlow with
# `when: {visible: "Not Now"}` so it only runs when the OS password-save
# sheet is actually on screen. Never referenced unconditionally.
- tapOn: "Not Now"
