# Phase 22 Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans inline, task-by-task. The learner explicitly prohibited subagents for this work. Steps use checkbox syntax for tracking.

**Status:** Implemented and reviewed inline on 2026-09-18. Local checks passed; live acceptance remains pending. No subagents used.

**Goal:** Deliver optional encrypted registration names across web/iOS, API and PostgreSQL, plus a separate synthetic-PII KMS lifecycle lab. Distinguish local checks from live acceptance.

**Architecture:** The API uses the KMS SDK with a separate sandbox application key to encrypt names and decrypt authenticated greetings. A standalone Terraform root and stdlib runner support isolated lifecycle drills.

**Tech Stack:** Existing FastAPI/PostgreSQL and Expo stack, Google Cloud KMS SDK/CRC32C, Python stdlib/unittest, Google Cloud CLI, Terraform 1.14.7, Google provider 8.2.0.

**Spec:** [Phase 22 design](../specs/2026-09-18-cloud-kms-design.md).

## Full-stack extension (learner-approved)

Status: implemented and verified locally, inline without subagents in the same worktree/branch. Live cloud and native-device acceptance remain pending.

1. **API and persistence:** Add `app/name_encryption.py`, KMS/CRC32C dependencies and lockfile; additive migration `2026091801_user_real_name.py`; update `auth_repository.py` and auth schemas/routes in `main.py`. Test SDK integrity/AAD with a fake KMS boundary and real PostgreSQL signup/login/me, no plaintext persistence, no-account-on-encrypt-failure, no decrypt before authentication, fallback on KMS failure, auth response/input privacy, and legacy accounts before implementation.
2. **Shared web/iOS UI:** Extend `AuthScreen.tsx`, `AuthProvider.tsx`, and `todoApi.ts` with optional name input and strict optional response validation. Keep existing signup transport options compatible. Add form, transport and greeting/restoration tests before implementation. Update existing greeting expectations.
3. **Infrastructure:** Add `sandbox/profile-encryption.tf` and a default-false variable; wire API-only environment and IAM dependency into `run.tf`; add mock checks for disabled/enabled and role isolation. Keep the lab key separate, no duplicate API ownership. Document ordering and the need to keep keys on rollback.
4. **Integration and docs:** Extend the test-only E2E entry point with a fake KMS client (never production), add an actual registration/login/refresh browser journey, update spec/guide/roadmap and local checks. Run API/mobile suites, typecheck/lint, focused E2E and Terraform checks. Self-review and commit on the existing branch. Keep live cloud and native-device acceptance pending unless observed.

Exact data/config/API contracts are in the spec's primary-use-case section. Test commands: `uv run --directory apps/api python -m pytest tests`; `pnpm --dir apps/mobile test --runInBand`; `pnpm --dir apps/mobile typecheck`; Terraform sandbox/lab mock tests; focused Playwright registration journey.

## Global constraints

- Use invented data for acceptance; add only the approved optional encrypted-name application feature, retaining the separate synthetic fixture lab.
- Terraform 1.14.7; Google provider 8.2.0; the full-stack extension adds only the KMS SDK and CRC32C integrity helper.
- Key ring `phase22-lab`, key `synthetic-pii`, software symmetric encryption.
- Rotation 7776000 seconds; destruction scheduling 2592000 seconds.
- Key `prevent_destroy = true` and `deletion_policy = "PREVENT"`.
- No downloaded service-account keys, project-wide crypto grants, lifecycle commands in the runner, or cloud credentials in CI.
- Keep `.pi/` and untracked root `AGENTS.md` untouched. Work in `.worktrees/phase-22-cloud-kms` on `codex/phase-22-cloud-kms`, based on `a0a9a5e`.
- Local implementation does not constitute cloud or learner acceptance. No subagents, including review.

## Task 1: Isolated infrastructure

**Files:** Create `infra/terraform/kms-lab/{main.tf,versions.tf,backend.tf,outputs.tf,terraform.tfvars.example,backend.hcl.example,.terraform.lock.hcl,tests/safety.tftest.hcl}`.

**Interface:** Explicit `project_id`, `region`, `operator_member` inputs. Outputs `key_id`, `reader_email`, `writer_email`. One GCS prefix separate from sandbox.

- [x] Write Terraform mock-plan assertions before resources: symmetric software key, rotation/recovery window, protected deletion policy, key-scoped grants, service-account-scoped impersonation, rejected empty/invalid inputs.
- [x] Run tests and observe missing resources/variables fail before implementation.
- [x] Reuse pinned versions and lockfile; implement the resources and explicit input validation. Core key settings:

```hcl
rotation_period            = "7776000s"
destroy_scheduled_duration = "2592000s"
deletion_policy            = "PREVENT"
version_template {
  algorithm        = "GOOGLE_SYMMETRIC_ENCRYPTION"
  protection_level = "SOFTWARE"
}
lifecycle {
  prevent_destroy = true
}
```

- [x] Run `terraform -chdir=infra/terraform/kms-lab init -backend=false -lockfile=readonly`, `fmt -check`, `validate`, and `test -no-color`. No real backend or cloud credentials.

## Task 2: Fixture runner, test first

**Files:** Create `scripts/kms_lab.py`, `tests/test_kms_lab.py`; modify `.gitignore` and `package.json`.

**Interface:** `seal(key_version: str, identity: str, bundle: Path) -> None`, `check(identity: str, bundle: Path) -> None`, CLI commands matching the spec. Only `seal` accepts `--key-version`.

- [x] Add a unittest using a subprocess-boundary fake that reads/writes the real temporary files. A successful fake round trip must produce a saved JSON bundle and reject changed plaintext on check. The fake is not an encryption implementation or proof of KMS behavior.
- [x] Add cases for wrong key/identity project, malformed metadata/base64, oversized input, extra fields, timeout/nonzero command failure, no-clobber output, and temp-file cleanup. Corrupting AAD/ciphertext must reach a simulated KMS rejection, not succeed locally.
- [x] Run `python3 -m unittest discover -s tests -p test_kms_lab.py -v`; observe failure before implementing the runner.
- [x] Implement with `argparse`, `base64`, `json`, `pathlib`, `re`, `subprocess`, `tempfile` and `os`. Keep fixture bytes in one constant; serialize AAD from schema/key-version with sorted keys and compact separators. Pass gcloud explicit project, identity, keyring, location and file arguments; encrypt also receives the numeric version. Set a 60-second command timeout and preserve gcloud integrity checks.
- [x] Validate input before network calls. Limit bundles to 32 KiB and decoded ciphertext to 16 KiB; require positive numeric versions and same-project lab identities. Use private temporary directories and exclusive output creation. Print only a safe success/failure status, not payloads or raw tool errors.
- [x] Run the tests, runner `--help`, and Ruff with the existing API configuration. Add `pnpm test:kms` and ignore `artifacts/kms-lab/`.

## Task 3: Walkthrough and recovery evidence

**Files:** Create `docs/guides/22-cloud-kms.md`; update `README.md`, `docs/curriculum-roadmap.md`, `docs/workflow-learning-plan.md`, `infra/terraform/README.md`.

- [x] Explain the four encryption/secret controls and the fintech threat-model questions.
- [x] Include explicit project/region/state prerequisites, unique state ownership, audit `DATA_READ` prerequisite, planned-resource review, costs, and impersonation limitations.
- [x] Provide copyable commands for primary capture, seal/check, writer encryption/denied decrypt, rotation, unchanged old-bundle hash, disable/re-enable, verified replacement, and unused-version destroy/restore. Quote shell variables and use explicit resource selectors.
- [x] Include interruption recovery, dependency/destruction checklist, no automatic destructive cleanup, and final state inspection. Every expected negative result must be diagnosed, not counted merely because exit status is nonzero.
- [x] Mark all cloud evidence unobserved; record actual local verification separately. Update curriculum links without rewriting unrelated phase acceptance history.

## Task 4: CI and final verification

**Files:** Modify `.github/workflows/quality.yml`; update the plan and guide with observed results.

- [x] Extend the existing Terraform job to init/validate/mock-test this root. Add `pnpm test:kms` and runner/test Ruff checks in the application job; no live KMS tests.
- [x] Run the focused unittest suite, Terraform fmt/validate/mock tests, repository contracts and changed-file Markdown checks. Check local relative links and official external references.
- [x] Review the full diff inline against the spec, concentrating on impersonation, path/format validation, no-clobber writes, partial failures, backend isolation, and recovery commands. No subagent review.
- [x] Commit only phase 22 files on its branch after checks. Keep the worktree available; do not merge or claim live acceptance.

## Live acceptance handoff

- [ ] Review explicit cloud project, location, backend and budget; resolve API and audit-policy ownership before apply.
- [ ] Provision lab resources and follow Guide 22. Record observed evidence and restore safe states after each drill.
- [ ] Obtain learner sign-off only after the live acceptance table is complete; retain any unobserved rows as pending.
